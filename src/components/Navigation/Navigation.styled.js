import styled from 'styled-components';

const Nav = styled.nav`
  display: flex;
  align-items: center;
  gap: 30px;
`;

const Logo = styled.span`
  font-size: 22px;
  color: #61dafb;
  margin-right: 50px;
`;

export { Nav, Logo };
